name: Terraform Mock

on: 
  push: 
    branches:
      - develop
      - main
  workflow_dispatch:


jobs:
  terraform:
    name: 'Terraform Validate'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3